language: python
python:
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"

install:
    - pip install -r test_requirements.txt
    - pip install -r requirements.txt
    - pip install -e .

script:
    - py.test

# after_success:
#     - coveralls
